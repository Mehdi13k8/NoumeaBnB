/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath: '/noumea',
  basePath: "",
  images: {
    domains: ["picsum.photos"],
  },
  output: 'export',
};

export default nextConfig;
